<div class="table-header"><div class="table-header r"><div class="table-header m"><h2>Tapahtumatyypit</h2></div></div></div>
<div class="border">
<div id="buttonbar">
	<%= button_to ("Alusta tapahtumat", {:action => 'create_standard_occasions'}) if permitted_to? :manage, :occasion_types %>
</div>

<%= crumbs %>
  
<table style="width: 95%;">
  <thead>
  	<tr>
		<th>Tyyppi</th>
		<th>Julkinen?</th>
		<th>&nbsp;</th>
	</tr>
  </thead>

<tbody>
<% @occasion_types.each do |occasion_type| %>
  <tr class="<%= cycle('even', 'odd') %>">
    <td><%= occasion_type.name %></td>
    <% hidden_field("occasion_type", "name")%>
   <td><%= check_box("occasion_type","visibility",{},"private","public")%></td>
 </tr>
<% end %>
</tbody>
</table>

<br />
</div>
